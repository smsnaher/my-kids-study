import React from 'react';

const SubtractionQuestion: React.FC = () => (
  <div>
    {/* Subtraction question form fields go here */}
    <label>
      Enter numbers to subtract:
      <input type="text" name="subtractionInput" />
    </label>
  </div>
);

export default SubtractionQuestion;